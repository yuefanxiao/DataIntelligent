# OS
.DS_Store

# Go
bin/
*.test

# Runtime state（SQLite 运行时存储，含 -wal/-shm 伴随文件）
dgw.db
dgw.db-*

# Node
node_modules/

# Env / secrets
.env
.env.*
*.pem
*.key

# 部署凭证 env 文件（ADR-0009：凭证只在宿主机该文件，不入 git）
deploy/config/env
deploy/demo/.env
deploy/demo/run/

# demo 运行产物（执行记录/运行时存储）
deploy/demo/logs/
deploy/demo/dgw.db*
